[project]
name = "local_database"
version = "0.1.0"
description = "Database locale basato su file JSON e TXT"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"